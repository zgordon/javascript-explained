/*
 * Create a new string variable called fullName and combine
 * firstName and lastName together with a space between, using
 * the plus sign + to combine the strings. Log the full name to
 * the console to test it.
 */

const fullName = firstName + ` ` + lastName;

console.log(fullName);
