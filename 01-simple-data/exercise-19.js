/*
 * Create a new variable called isBlacklisted and have it return true if comment contains the word
 * "notallowed" and false if the word does not exist.  Log the values out to the console to test.
 */

const isBlacklisted = comment.includes(`notallowed`);

console.log(isBlacklisted);
