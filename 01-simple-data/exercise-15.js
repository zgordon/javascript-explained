/*
 * Create a new variable called myLink with markup for a link.
 * Include the variables linkURL for the href value and linkText for
 * the link text.
 *
 *  Example:
 * const demoTag = `<tag attr="${demoAttr}" >${demoTitle}</tag>`
 */

const myLink = `<a href"${linkURL}">${linkText}</a>`;

console.log(myLink);
