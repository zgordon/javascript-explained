/*
 * Create a new variable called testURL and assign it the value of https://test.dev#clicked . Get the
 * position of # and log it to the console.
 */

const testURL = `https://test.dev#clicked`;

const indexOfHash = testURL.indexOf(`#`);

console.log(indexOfHash);
