/*
 * Create a new variable called isInLyrics and have it use .includes() to return true if a certain
 * word is in the lyrics and false if a certain word is not in the lyrics.  Log the results to
 * the console to test.
 */

const isInLyrics = lyrics.includes("Church");

console.log(isInLyrics);
