/*
 * Create a new string variable called myHeader with markup for an
 * h1 that uses the headerTitle variable for the text.
 *
 * Example: const demoHeader =  `<h1>${demoTitle}</h1>`
 */

const myHeader = `<h1>${headerTitle}</h1>`;

console.log(myHeader);
