/*
 * Write a function called getFullName() that takes two parameters, first and last , combines them with
 * a space between and then returns them.
 */

function getFullName(first, last) {
	return `${first} ${last}`;
}
