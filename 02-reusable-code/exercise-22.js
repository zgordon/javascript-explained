/*
 * Create a function called getArticleMarkup() that takes the following parameters: articleTitle,
 * articleURL, articleExcerpt, articleAuthor .  Then inside of the function create a variable called
 * markup that gets the value of getHeaderLinkMarkup() and adds to it the value of getExcerptMarkup()
 * and finally adds to it the value of getAuthorMarkup() .  Then return all of that combined markup.
 * Log getArticlelMarkup() to the console and pass it the correct parameters to test.
 *
 * Example:
 *  function getMarkup() {
 *      let markup = getMarkup1()
 *      markup += getMarkup2()
 *      markup += getMarkup3()
 *      return markup
 *   }
 */
