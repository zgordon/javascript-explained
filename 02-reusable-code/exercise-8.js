/*
 * Create a variable called fullName and assign it the value of getFullName() with your first and last
 * name passed as the two parameters.  Log fullName to the console to test.
 */

const fullName = getFullName(`Harry`, `Potter`);

console.log(fullName);
