/*
 * Create a function called getMyHeaderMarkup( title ) that takes a parameter of title and returns a
 * string of markup with an <h1> and the title passed in.  Console log getHeaderMarkup() and pass
 * it some header text to test.
 */
