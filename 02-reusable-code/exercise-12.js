/*
 * Create a function called getLogin() that does the same thing as login() but it also returns true
 * or false depending on whether the person logged in successfully.  Log getLogin() to the console
 * to test it.
 */
