/*
 * Modify the init() function by adding a conditional check to see if loggedIn is equal to true.  If it is,
 * create a variable called name and set it equal to askFullName() from before.  Alert out a welcome
 * like "Welcome YOURNAME!".
 */
