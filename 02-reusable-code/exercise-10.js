/*
 * Create a new variable called userFullName and assign it the value of askFullName() . This should 
 trigger askFullName() to execute.  Enter the prompts and then log the value of userFullName to 
 * the console.
 */

const userFullName = askFullName();

console.log(userFullName);
