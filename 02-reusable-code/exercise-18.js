/*
 * Create a function called getMyLinkMarkup( title, url ) that takes two parameters, a url and a title.
 * Then have the function return something like this: <a href="URL">TITLE</a>. Log out getMyLinkMarkup()
 * in the console and pass it a URL and a title to test.
 */
