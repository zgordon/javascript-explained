/*
 * Write a function called getUsername() that returns the value of "myusername"".  Log getUsername() in
 * the console to test it.
 */

function getUsername() {
	return `myusername`;
}

console.log(getUsername());
