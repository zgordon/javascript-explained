/*
 * Create a variable called username and assign it to the value of getUsername() log the value to
 * the console.  It should equal "myusername."
 */

const username = getUsername();

console.log(username);
