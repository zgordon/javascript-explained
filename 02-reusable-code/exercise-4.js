/*
 * Write a function named askName() that asks for the user's name and then alerts a "Hi YOURNAME!".
 * Call askName() to test it.
 */

function askName() {
	const name = prompt("Please enter your name", "Harry Potter");
	alert(name);
}
askName();
